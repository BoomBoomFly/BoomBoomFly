# 任务与控制架构现状及讨论记录

记录日期：2026-09-19。

本记录是 2026-09-19 实现前的只读讨论快照。文中的“尚未实现”和待讨论项描述当时状态；当前代码、接口、运行命令与验证证据以 [PX4 SITL 起飞—悬停—降落测试记录](PX4_SITL_起飞悬停降落验证.md) 为准。

## 1. 已确定的职责边界

| 功能包 | 目标职责 |
| --- | --- |
| `uav_mission` | 决定当前任务阶段、目标以及成功、失败和取消条件 |
| `uav_control` | 封装控制通信、命令请求、状态反馈及周期 setpoint 发送 |
| `uav_vio_bridge` | 将 OpenVINS 位姿转发至 MAVROS，定位链路独立于任务控制 |
| `uav_bringup` | 组织 launch 和 YAML 配置，不实现任务或算法 |

计划中的控制调用关系：

```text
MissionNode → FlightTask → FlightControlInterface → MavrosInterface → MAVROS → PX4
```

任务层只依赖统一控制能力，不直接处理 MAVROS 或 DDS 消息。任务层更新目标，控制层持续发送目标；离散命令的请求发送与实际执行完成必须区分。

第一阶段目标是等待定位和飞控就绪、准备并进入 Offboard、解锁、起飞、悬停、降落、确认上锁及结束。具体顺序、转移条件和异常处理仍需讨论。暂不引入 `MissionExecutor`、多任务插件框架或自定义底层控制器。

## 2. 当前代码实际状态

### uav_mission

- 已有 `flight_task.hpp`、`mission_common.hpp` 和两个源文件。
- 缺少 `package.xml` 和 `CMakeLists.txt`，尚不是完整可构建的 ROS 包。
- `config/` 和 `launch/` 尚无 README 所述任务配置与启动文件。
- `src/flight_task.cpp` 为空；`src/mission_node.cpp` 仅有类声明，缺少必要 include、方法实现及运行入口。
- `mission_common.hpp` 的枚举位于 `uav_common` 命名空间；`flight_task.hpp` 在 `uav_mission` 中直接使用这些类型，未限定或引入命名空间，存在类型解析问题。
- `FlightTask` 引用的 `uav_control::FlightControlInterface` 尚未定义。

### uav_control

- 当前构建目标为 `mavros_interface_node`，由 `main.cpp` 和 `mavros_interface.cpp` 组成。
- `MavrosInterface` 直接继承 `rclcpp::Node`，当前只有状态订阅和固定位置 setpoint 发布。
- 启动时读取目标与频率，默认目标为本地 ENU `(0, 0, 2)` 米，默认发布频率为 20 Hz；朝向四元数固定为单位四元数。
- `/mavros/state` 用于记录连接、模式与解锁状态，目前没有向任务层提供统一状态查询接口。
- 尚无解锁、模式切换、降落请求、位置和速度反馈、着陆状态，以及供任务动态更新目标的接口。
- `flight_control_interface.hpp` 只有空命名空间；`config/control.yaml` 为空。当前生效的配置和启动文件是 `setpoint.yaml`、`setpoint.launch.py`。
- CMake 仅构建可执行程序，未提供控制库、公共头文件安装和供任务包使用的导出目标。
- DDS 类型为不可实例化的预留类型，源文件不参与当前构建。
- **用户已删除 `src/flight_task.cpp`，本次已确认该文件不存在。** 任务职责归入 `uav_mission`，不再将此文件列为待清理项。

固定发布高度目标不代表已经实现自动起飞，也不代表飞控已经接受目标。

### uav_vio_bridge

- 当前构建使用 `src/uav_vio_bridge_node.cpp`。
- 输入为 `/ov_msckf/poseimu`，输出为 `/mavros/vision_pose/pose_cov`，均为 `PoseWithCovarianceStamped`。
- 回调原样转发完整消息，保留时间戳、frame_id、位姿和协方差，不执行坐标变换。
- 另外存在空的 `src/uav_vio_bridge.cpp` 和头文件，不参与当前节点实现。

### uav_bringup

- 当前调用关系为 `uav.launch.py → vio.launch.py → uav_vio_bridge_node`。
- 通过本包 YAML 配置桥接话题。
- 尚未编排任务、控制、OpenVINS、MAVROS 或相机节点。

## 3. 文档与验证边界

- 根 README 尚未纳入 `uav_mission`；控制包 README 中的统一接口、命令能力和目录结构包含目标设计，不能全部视为当前实现。
- 本轮已检查现有 launch 的 Python 语法及三个已有 ROS 包的 XML 可解析性。
- 本轮未重新编译，未运行节点、消息转发测试、SITL 或实机测试。
- 包 README 中的历史编译、启动和测试记录保持原有证据范围，不作为本轮新增验证。
- 源码接口匹配不证明通信成功；位姿消息到达也不证明 PX4 已正确融合或可以飞行。

## 4. 参考仓库使用边界

用户指定的主线分工：

- 微空教程及 OpenVINS：定位、飞控 IMU、MAVROS 和外部视觉融合。
- Fast-Drone-250：定位、规划、控制连接，启动流程、实机调试及 px4ctrl；不直接照搬旧 ROS/PX4 参数。
- EGO-Swarm ROS 2：后续局部建图、避障和重规划。
- Kalibr：相机与 IMU 标定及 ROS 2 数据处理。

补充参考按需使用：Prometheus 的任务接口与状态管理，mavros_controllers 的轨迹生成与跟踪分离，TJU UAV_Simulator 的轨迹接口，Ctrl_Log_Plot 的日志和误差分析；PID_Controller、YOPO、GCOPTER、Primitive-Planner、Nav2 及 DDS 相关仓库属于后续对应需求的参考，不直接全部加入工作空间编译。

本轮核对的本地依赖快照：

| 依赖 | 本地分支与提交 | 已核对内容 |
| --- | --- | --- |
| OpenVINS | `master@6948812` | ROS 2 `poseimu` 发布类型与桥接输入一致；该分支不是指定的 `micoair-mvd35` |
| MAVROS | `ros2@e4f39208` | 视觉插件提供 `pose_cov` 订阅路径，并包含坐标、姿态及协方差转换 |

以上是本地快照，不代表上游最新版本，也不代表已完成全部参考仓库审计。采用具体实现前仍需核对所选分支、依赖和 ROS/PX4 版本，以及坐标、时间戳、标定和融合配置。

## 5. 下一步讨论顺序

### 先确定任务与控制如何连接

当前 `FlightTask` 的构造函数接受控制接口引用，表达的是同一进程内直接调用 C++ 对象的设计；独立的控制可执行程序本身无法通过这个引用被另一个进程直接调用。

待讨论两种方式：

| 方式 | 需要明确的内容 |
| --- | --- |
| 同一进程内装配任务与控制 | 控制实现作为库导出；谁创建并持有后端对象；ROS 回调和两个更新周期如何被驱动 |
| 独立进程，通过 ROS 接口连接 | 任务命令、目标和状态的 topic/service/action 契约；若保留 C++ 接口，谁实现通信代理 |

**讨论起点建议：优先考虑同一进程内装配。** 它贴近现有接口引用设计，第一阶段所需通信环节较少。包可以保持独立；进程组织方式无需与包一一对应。这只是待讨论建议，尚未确定实施，也尚未决定独立控制节点的最终用途。

无论采用哪种方式，都需明确控制发布的唯一所有者，避免旧固定目标节点与任务控制同时写入 setpoint。

### 再确定最小控制契约

- 状态：连接、解锁、模式、着陆状态、位置，以及定位有效性和数据新鲜度；未知状态不能默认等同于有效或已着陆。
- 命令：请求是否发出、是否被接受、实际状态是否达到，分别由谁记录与判断；超时和重试归属要清楚。
- 目标：坐标系、单位、航向，以及起飞高度是相对起飞点还是本地坐标绝对高度。
- 周期：任务更新与 setpoint 发布各由谁驱动；取消或失败后当前目标如何处置。

### 最后落实第一版状态机的规则

先定义每个阶段的进入动作、完成条件和超时处理，再讨论实现。重点包括：定位就绪如何判定、何时准备 Offboard、如何确认起飞和降落，以及取消任务时如何根据地面或空中状态处理。任务结果与飞行处置应分清，不能仅停止任务更新就假定飞机已安全结束飞行。

后续验证应逐级区分：可编译、主机任务逻辑检查、ROS 通信、PX4 SITL 飞行闭环、实机验证。当前尚未推进这些新增验证。
